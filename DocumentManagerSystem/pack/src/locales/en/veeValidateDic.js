import attributes from './../en.json'

export default {
  // 對應到輸入元素的name名稱 <input name="__userId"> or <input data-vv-name="__userId">
  // 在上方 error message 的 field 就會自動取代成"英文"語系檔中 key 為 __userId 的文字
  attributes
}
